init:
  - name: tnpm-install

dev:
  - name: shell
    script:
      - node ./lib/index.js

build:
  - name: shell
    script:
      - dn dev

test:
  - name: lint

publish:
  - name: shell
    script:
      - tnpm pu @ali/${name}