init:
  - name: shell
    script:
      - tnpm i

dev:
  - name: shell
    script:
      - node ./lib/index.js

build:
  - name: shell
    script:
      - node ./lib/index.js

publish:
  - name: shell
    script:
      - tnpm pu @ali/${name}