init:
  - name: tnpm-install
  - name: pkginfo

dev:
  - name: shell
    script:
      - node ./lib/index.js

build:
  - name: shell
    script:
      - dn dev

test:
  - name: lint

publish:
  - name: shell
    script:
      - tnpm pu ${name}